# Cross-Module Scenarios: Migration x Staking (XG-001 to XG-050)

**Modules:** SSVClusters.migrateClusterToETH x SSVStaking (stake/syncFees/claimEthRewards)
**Prefix:** XG
**Source:** `contracts/modules/SSVClusters.sol:259-343`, `contracts/modules/SSVStaking.sol:36-228`, `contracts/libraries/ProtocolLib.sol:22-151`, `contracts/libraries/OperatorLib.sol:233-384`
**Cross-refs:** MG-* (migration W1), ST-* (staking W1), EB-* (effective balance), LQ-* (liquidation), RM4-* (removed operator)
**Worker:** Wave 2 — cross-module interactions

---

## Tag Legend

| Key | Values | Meaning |
|-----|--------|---------|
| `entry` | functionName(s) | Contract entry points under test (chained with `+`) |
| `version` | `ssv->eth` / `eth` / `both` | Fee version context |
| `eb` | `implicit` / `explicit` / `mixed` | Effective-balance mode |
| `cluster` | `active` / `liquidated` / `migrated` / `mixed` | Cluster state(s) involved |
| `ops` | `4` / `7` / `13` / `parametric` | Operator count |
| `stakers` | `1` / `2` / `3` / `many` | Number of cSSV stakers |
| `remove_mode` | `real` / `none` | Operator removal mode |
| `revert` | `yes` / `no` | Whether scenario expects a revert |

---

## Scenario Table

| ID | Flow | Purpose | Tags | Tested | File References |
|----|------|---------|------|--------|-----------------|
| XG-001 | Stake cSSV -> migrate SSV cluster to ETH -> syncFees -> claimEthRewards | Happy path: staker claims rewards generated by a newly migrated cluster; verifies migration increases `daoTotalEthVUnits` and rewards flow to stakers | `entry:stake+migrateClusterToETH+syncFees+claimEthRewards; version:ssv->eth; eb:implicit; cluster:active; ops:4; stakers:1; remove_mode:none; revert:no` | [ ] | SSVClusters.sol:259-343, SSVStaking.sol:43-60+113-151, ProtocolLib.sol:84-89+107-119 |
| XG-002 | Migrate SSV cluster to ETH -> stake cSSV -> advance blocks -> syncFees -> claim | Staker enters AFTER migration; rewards accrue only from stake block forward, not retroactively from migration | `entry:migrateClusterToETH+stake+syncFees+claimEthRewards; version:ssv->eth; eb:implicit; cluster:active; ops:4; stakers:1; remove_mode:none; revert:no` | [ ] | SSVClusters.sol:287, SSVStaking.sol:43-60+113-151, ProtocolLib.sol:84-89 |
| XG-003 | Two stakers (pre/post migration) -> migrate -> syncFees -> both claim | Proportional split: staker A enters before migration (gets SSV-era fees), staker B enters after; verify each gets correct share of post-migration ETH fees | `entry:stake+migrateClusterToETH+syncFees+claimEthRewards; version:ssv->eth; eb:implicit; cluster:active; ops:4; stakers:2; remove_mode:none; revert:no` | [ ] | SSVStaking.sol:113-151+199-206, ProtocolLib.sol:84-89+107-119 |
| XG-004 | Migrate cluster -> updateClusterBalance (explicit EB > baseline) -> syncFees -> claim | Migration with implicit EB, then oracle increases EB; verify `daoTotalEthVUnits` grows and reward rate increases for stakers | `entry:migrateClusterToETH+updateClusterBalance+syncFees+claimEthRewards; version:ssv->eth; eb:implicit->explicit; cluster:active; ops:4; stakers:1; remove_mode:none; revert:no` | [ ] | SSVClusters.sol:259-343+348-417, ProtocolLib.sol:84-89+142-150, SSVStaking.sol:183-207 |
| XG-005 | Migrate cluster with explicit EB already set -> syncFees -> claim | Pre-migration `updateClusterBalance` sets vUnits > baseline on SSV cluster; migration carries deviation into `daoTotalEthVUnits`; staker claims at elevated rate | `entry:updateClusterBalance+migrateClusterToETH+syncFees+claimEthRewards; version:ssv->eth; eb:explicit; cluster:active; ops:4; stakers:1; remove_mode:none; revert:no` | [ ] | SSVClusters.sol:304-327, ProtocolLib.sol:84-89, SSVStaking.sol:183-207 |
| XG-006 | Migrate cluster -> network fee change -> syncFees -> claim | Fee model transition: migration uses initial ETH network fee; admin changes fee; staker's rewards reflect both fee rates across the two periods | `entry:migrateClusterToETH+updateNetworkFee+syncFees+claimEthRewards; version:ssv->eth; eb:implicit; cluster:active; ops:4; stakers:1; remove_mode:none; revert:no` | [ ] | ProtocolLib.sol:40-46+84-89, SSVStaking.sol:183-207, SSVClusters.sol:259-343 |
| XG-007 | Migrate cluster -> liquidate -> syncFees -> claim | Migrated cluster runs out of ETH and is liquidated; fee accrual stops; staker claims only pre-liquidation rewards | `entry:migrateClusterToETH+liquidate+syncFees+claimEthRewards; version:ssv->eth; eb:implicit; cluster:active->liquidated; ops:4; stakers:1; remove_mode:none; revert:no` | [ ] | SSVClusters.sol:31-65+552-612, ProtocolLib.sol:107-119, SSVStaking.sol:183-207+113-151 |
| XG-008 | Multiple stakers + migrate + liquidate -> proportional distribution | Three stakers with different cSSV balances; cluster migrates, generates fees, is liquidated; each staker claims proportional share of frozen rewards | `entry:stake+migrateClusterToETH+liquidate+syncFees+claimEthRewards; version:ssv->eth; eb:implicit; cluster:active->liquidated; ops:4; stakers:3; remove_mode:none; revert:no` | [ ] | SSVStaking.sol:199-206+113-151, SSVClusters.sol:552-612 |
| XG-009 | Migrate cluster -> liquidate -> reactivate -> syncFees -> claim full lifecycle | Full chain: SSV->ETH->liquidated->reactivated; staker rewards reflect gap during liquidation (no accrual) and resumed accrual after reactivation | `entry:migrateClusterToETH+liquidate+reactivate+syncFees+claimEthRewards; version:ssv->eth; eb:implicit; cluster:active->liquidated->active; ops:4; stakers:1; remove_mode:none; revert:no` | [ ] | SSVClusters.sol:259-343+31-65+129-181, ProtocolLib.sol:84-89+107-119, SSVStaking.sol:183-207+113-151 |
| XG-010 | Stake -> migrate -> requestUnstake (partial) -> syncFees -> claim | Partial unstake after migration; rewards accrue on reduced cSSV balance; verify proportional accuracy | `entry:stake+migrateClusterToETH+requestUnstake+syncFees+claimEthRewards; version:ssv->eth; eb:implicit; cluster:active; ops:4; stakers:1; remove_mode:none; revert:no` | [ ] | SSVStaking.sol:65-93+113-151, SSVClusters.sol:259-343 |
| XG-011 | Stake -> migrate -> requestUnstake (full) -> fees accrue -> stake again -> claim | BUG-6 interaction: full unstake drops cSSV supply to 0; fees generated by migrated cluster during zero-supply period are lost; re-staker cannot claim them | `entry:stake+migrateClusterToETH+requestUnstake+stake+syncFees+claimEthRewards; version:ssv->eth; eb:implicit; cluster:active; ops:4; stakers:1; remove_mode:none; revert:no` | [ ] | SSVStaking.sol:199-203 (BUG-6 path), SSVClusters.sol:259-343, ProtocolLib.sol:84-89 |
| XG-012 | Migrate with removed operator -> syncFees -> claim (burn rate vs reward rate divergence) | Removed operator contributes zero to burn rate but DAO `daoTotalEthVUnits` may include stranded deviation; verify staker rewards match actual network earnings, not inflated vUnits | `entry:migrateClusterToETH+syncFees+claimEthRewards; version:ssv->eth; eb:implicit; cluster:active; ops:4; stakers:1; remove_mode:real; revert:no` | [ ] | OperatorLib.sol:362-365, SSVClusters.sol:319-322, ProtocolLib.sol:84-89, SSVStaking.sol:183-207 |
| XG-013 | Migrate with removed op + explicit EB -> syncFees -> claim (stranded deviation) | Stranded `operatorEthVUnits` on removed op inflates `daoTotalEthVUnits`; `networkTotalEarnings` computes higher fees than actually collectible from clusters; staker pool balance may exceed what clusters can fund | `entry:migrateClusterToETH+syncFees+claimEthRewards; version:ssv->eth; eb:explicit; cluster:active; ops:4; stakers:1; remove_mode:real; revert:no` | [ ] | SSVClusters.sol:314-326 (deviation loop has no removed-op guard), ProtocolLib.sol:84-89, SSVStaking.sol:183-207 |
| XG-014 | Migrate multiple clusters in sequence -> syncFees -> claim (staking pool balance consistency) | Two SSV clusters migrate to ETH in separate txns; `daoTotalEthVUnits` accumulates both baselines + deviations; staker rewards reflect combined fee generation | `entry:migrateClusterToETH+migrateClusterToETH+syncFees+claimEthRewards; version:ssv->eth; eb:implicit; cluster:active; ops:4; stakers:1; remove_mode:none; revert:no` | [ ] | SSVClusters.sol:287 (updateDAO called twice), ProtocolLib.sol:107-119+84-89, SSVStaking.sol:183-207 |
| XG-015 | Migrate multiple clusters (shared operators) -> one liquidated -> syncFees -> claim | Shared operators serve two migrated clusters; one liquidated reduces `daoTotalEthVUnits`; staker reward rate drops proportionally | `entry:migrateClusterToETH+migrateClusterToETH+liquidate+syncFees+claimEthRewards; version:ssv->eth; eb:implicit; cluster:mixed; ops:4; stakers:1; remove_mode:none; revert:no` | [ ] | SSVClusters.sol:552-612 (updateDAO false), ProtocolLib.sol:107-119, SSVStaking.sol:183-207 |
| XG-016 | Large staking event -> small cluster liquidation -> precision check | Stake 1e24 SSV; tiny cluster generates minimal fees before liquidation; verify `accEthPerShare` increment rounds correctly and no phantom rewards appear | `entry:stake+migrateClusterToETH+liquidate+syncFees+claimEthRewards; version:ssv->eth; eb:implicit; cluster:active->liquidated; ops:4; stakers:1; remove_mode:none; revert:no` | [ ] | SSVStaking.sol:199-203 (precision floor), ProtocolLib.sol:84-89, SSVClusters.sol:552-612 |
| XG-017 | Migrate liquidated SSV cluster -> syncFees -> claim | Liquidated SSV cluster migrated to ETH (reactivation path); verify DAO accounting correct: `updateDAOSSV` NOT called, `updateDAO` IS called; staker rewards start from migration block | `entry:migrateClusterToETH+syncFees+claimEthRewards; version:ssv->eth; eb:implicit; cluster:liquidated; ops:4; stakers:1; remove_mode:none; revert:no` | [ ] | SSVClusters.sol:265+284-288+340-342, ProtocolLib.sol:107-119+84-89, SSVStaking.sol:183-207 |
| XG-018 | Migrate -> EB update triggers auto-liquidation -> syncFees -> claim | Migrate cluster; oracle updates EB downward (e.g., slashing); auto-liquidation fires in `_liquidateAfterEBUpdateIfNeeded`; verify staker rewards stop at liquidation block | `entry:migrateClusterToETH+updateClusterBalance+syncFees+claimEthRewards; version:ssv->eth; eb:implicit->explicit; cluster:active->liquidated; ops:4; stakers:1; remove_mode:none; revert:no` | [ ] | SSVClusters.sol:519-550+552-612, ProtocolLib.sol:107-119, SSVStaking.sol:183-207 |
| XG-019 | syncFees sandwich: syncFees -> migrate -> syncFees -> claim (index recalculation) | First sync captures pre-migration DAO state; migration changes `daoTotalEthVUnits`; second sync captures post-migration delta; verify `accEthPerShare` reflects both periods correctly | `entry:syncFees+migrateClusterToETH+syncFees+claimEthRewards; version:ssv->eth; eb:implicit; cluster:active; ops:4; stakers:1; remove_mode:none; revert:no` | [ ] | SSVStaking.sol:183-207 (_syncFees updates ethDaoIndexBlockNumber), ProtocolLib.sol:65-68+84-89, SSVClusters.sol:287 |
| XG-020 | Migrate cluster -> removeOperator from migrated cluster -> syncFees -> claim | Post-migration operator removal reduces cluster's burn rate but does NOT change `daoTotalEthVUnits` (baseline removed via `ethValidatorCount`, deviation may strand); staker reward rate unchanged if only baseline affected | `entry:migrateClusterToETH+removeOperator+syncFees+claimEthRewards; version:ssv->eth; eb:implicit; cluster:active; ops:4; stakers:1; remove_mode:real; revert:no` | [ ] | OperatorLib.sol:233-262 (updateClusterOperators), ProtocolLib.sol:84-89, SSVStaking.sol:183-207 |
| XG-021 | Migrate -> deposit ETH -> syncFees -> claim (deposit does not change fee accrual) | Post-migration deposit increases cluster balance but NOT `daoTotalEthVUnits`; staker reward rate unchanged by deposit | `entry:migrateClusterToETH+deposit+syncFees+claimEthRewards; version:ssv->eth; eb:implicit; cluster:active; ops:4; stakers:1; remove_mode:none; revert:no` | [ ] | SSVClusters.sol:186-201, ProtocolLib.sol:84-89, SSVStaking.sol:183-207 |
| XG-022 | Migrate -> withdraw ETH -> cluster near liquidation -> syncFees -> claim | Post-migration withdraw drains cluster balance close to liquidation threshold; cluster still active; staker rewards unaffected by withdrawal (fee rate unchanged) | `entry:migrateClusterToETH+withdraw+syncFees+claimEthRewards; version:ssv->eth; eb:implicit; cluster:active; ops:4; stakers:1; remove_mode:none; revert:no` | [ ] | SSVClusters.sol:206-253, ProtocolLib.sol:84-89, SSVStaking.sol:183-207 |
| XG-023 | Migrate -> cSSV transfer between stakers -> syncFees -> both claim | Settlement on cSSV transfer: both parties settled at migration-era `accEthPerShare`; post-transfer rewards distributed by new cSSV balances | `entry:migrateClusterToETH+stake+onCSSVTransfer+syncFees+claimEthRewards; version:ssv->eth; eb:implicit; cluster:active; ops:4; stakers:2; remove_mode:none; revert:no` | [ ] | SSVStaking.sol:173-181+209-228+113-151, SSVClusters.sol:259-343 |
| XG-024 | SSV-only cluster fees -> migrate -> staker claims ETH rewards from post-migration only | SSV clusters generate `daoBalance` (SSV), not `ethDaoBalance`; migration switches to ETH fees; verify staker rewards only come from ETH-era `networkTotalEarnings`, never SSV-era | `entry:stake+syncFees+migrateClusterToETH+syncFees+claimEthRewards; version:ssv->eth; eb:implicit; cluster:active; ops:4; stakers:1; remove_mode:none; revert:no` | [ ] | ProtocolLib.sol:84-89 (ETH earnings) vs ProtocolLib.sol:97-99 (SSV earnings), SSVStaking.sol:183-207 |
| XG-025 | Mixed SSV and ETH clusters -> migrate SSV cluster -> syncFees -> claim | Pre-existing ETH cluster generates fees; additional SSV cluster migrates; staker rewards increase by migrated cluster's contribution to `daoTotalEthVUnits` | `entry:registerValidator+migrateClusterToETH+syncFees+claimEthRewards; version:both; eb:implicit; cluster:active; ops:4; stakers:1; remove_mode:none; revert:no` | [ ] | ProtocolLib.sol:84-89+107-119, SSVClusters.sol:287, SSVStaking.sol:183-207 |
| XG-026 | Migrate cluster -> network fee set to zero -> syncFees -> claim | After migration, admin sets `ethNetworkFee = 0`; `networkTotalEarnings` stops growing; staker rewards freeze at last sync level | `entry:migrateClusterToETH+updateNetworkFee+syncFees+claimEthRewards; version:ssv->eth; eb:implicit; cluster:active; ops:4; stakers:1; remove_mode:none; revert:no` | [ ] | ProtocolLib.sol:40-46 (ethNetworkFee = 0), ProtocolLib.sol:84-89 (earningsUnits = 0), SSVStaking.sol:183-207 |
| XG-027 | Migrate cluster with 0 validators -> syncFees -> claim | Edge: cluster has 0 validators; migration calls `updateDAO(true, 0)` adding zero to `daoTotalEthVUnits`; no fee contribution; staker rewards unchanged | `entry:migrateClusterToETH+syncFees+claimEthRewards; version:ssv->eth; eb:implicit; cluster:active; ops:4; stakers:1; remove_mode:none; revert:no` | [ ] | SSVClusters.sol:287 (updateDAO with 0), ProtocolLib.sol:109 (vUnitsDelta = 0), SSVStaking.sol:183-207 |
| XG-028 | Stake -> migrate -> EB update increases vUnits -> syncFees -> claim -> EB update decreases vUnits -> syncFees -> claim | Two-phase EB change: first increase raises reward rate, second decrease lowers it; verify each claim reflects correct period's rate | `entry:stake+migrateClusterToETH+updateClusterBalance+syncFees+claimEthRewards+updateClusterBalance+syncFees+claimEthRewards; version:ssv->eth; eb:explicit; cluster:active; ops:4; stakers:1; remove_mode:none; revert:no` | [ ] | SSVClusters.sol:386-417, ProtocolLib.sol:142-150+84-89, SSVStaking.sol:183-207+113-151 |
| XG-029 | Migrate -> EB update from 32 ETH to 2048 ETH (MAX) -> syncFees -> claim (max vUnits) | Maximum EB per validator; `daoTotalEthVUnits` grows by large deviation; verify no overflow in `networkTotalEarnings` computation and staker receives correct large reward | `entry:migrateClusterToETH+updateClusterBalance+syncFees+claimEthRewards; version:ssv->eth; eb:explicit; cluster:active; ops:4; stakers:1; remove_mode:none; revert:no` | [ ] | SSVClusters.sol:453-458 (MAX_EB check), ProtocolLib.sol:84-89 (_safeUint64 guard), SSVStaking.sol:183-207 |
| XG-030 | Migrate -> ethDaoBalance grows -> claimEthRewards checks both stakingEthPoolBalance and ethDaoBalance | Verify double-guard: claim fails if `packedPayout > stakingEthPoolBalance` OR `packedPayout > ethDaoBalance`; migration drives both balances through `networkTotalEarnings` | `entry:migrateClusterToETH+syncFees+claimEthRewards; version:ssv->eth; eb:implicit; cluster:active; ops:4; stakers:1; remove_mode:none; revert:no` | [ ] | SSVStaking.sol:136-141 (double guard), ProtocolLib.sol:65-68+84-89 |
| XG-031 | Migrate -> operator fee change (declareOperatorFee + executeOperatorFee) -> syncFees -> claim | Post-migration operator fee change alters cluster burn rate but not `daoTotalEthVUnits`; staker reward rate (driven by network fee * vUnits) unchanged by operator fee change | `entry:migrateClusterToETH+declareOperatorFee+executeOperatorFee+syncFees+claimEthRewards; version:ssv->eth; eb:implicit; cluster:active; ops:4; stakers:1; remove_mode:none; revert:no` | [ ] | ProtocolLib.sol:84-89 (only uses ethNetworkFee, not operator fees), SSVStaking.sol:183-207 |
| XG-032 | Migrate cluster -> liquidate (ETH to liquidator) -> verify staking pool balance consistency | Liquidation transfers cluster's remaining ETH to liquidator; this ETH is NOT from the staking pool; verify `stakingEthPoolBalance` and `ethDaoBalance` remain self-consistent after liquidation | `entry:migrateClusterToETH+liquidate+syncFees; version:ssv->eth; eb:implicit; cluster:active->liquidated; ops:4; stakers:1; remove_mode:none; revert:no` | [ ] | SSVClusters.sol:599-609 (transferBalance to liquidator), SSVStaking.sol:183-207 (stakingEthPoolBalance tracks networkTotalEarnings) |
| XG-033 | Migration + staking race: migrate in block N, stake in block N, syncFees in block N | Same-block atomicity: migration updates `daoTotalEthVUnits` at block N; staking's `_syncFees` also at block N; verify no double-counting or zero-division edge | `entry:migrateClusterToETH+stake+syncFees; version:ssv->eth; eb:implicit; cluster:active; ops:4; stakers:1; remove_mode:none; revert:no` | [ ] | ProtocolLib.sol:84-89 (block.number - ethDaoIndexBlockNumber = 0 if same block), SSVStaking.sol:183-207 |
| XG-034 | Migrate -> liquidate -> all validators removed from DAO -> syncFees with `daoTotalEthVUnits = 0` | After liquidation zeroes out DAO counts, `networkTotalEarnings` returns just `ethDaoBalance` (no new accrual); `_syncFees` gets `current <= previous`; `accEthPerShare` unchanged | `entry:migrateClusterToETH+liquidate+syncFees; version:ssv->eth; eb:implicit; cluster:active->liquidated; ops:4; stakers:1; remove_mode:none; revert:no` | [ ] | ProtocolLib.sol:85-89 (units = 0 => earningsUnits = 0), SSVStaking.sol:191-194 (current <= previous path) |
| XG-035 | Migrate + explicit EB -> liquidate -> verify deviation removed from daoTotalEthVUnits | Explicit EB cluster liquidation removes deviation from `daoTotalEthVUnits` and `operatorEthVUnits`; subsequent `networkTotalEarnings` reflects reduced vUnits; staker reward rate drops correctly | `entry:migrateClusterToETH+updateClusterBalance+liquidate+syncFees+claimEthRewards; version:ssv->eth; eb:explicit; cluster:active->liquidated; ops:4; stakers:1; remove_mode:none; revert:no` | [ ] | SSVClusters.sol:564-596 (deviation removal in _executeLiquidation), ProtocolLib.sol:84-89, SSVStaking.sol:183-207 |
| XG-036 | Migrate + explicit EB -> reactivate -> verify deviation restored to daoTotalEthVUnits | After liquidation removes deviation, reactivation restores it; staker reward rate returns to pre-liquidation level | `entry:migrateClusterToETH+updateClusterBalance+liquidate+reactivate+syncFees+claimEthRewards; version:ssv->eth; eb:explicit; cluster:active->liquidated->active; ops:4; stakers:1; remove_mode:none; revert:no` | [ ] | SSVClusters.sol:142-154 (reactivation deviation accounting), ProtocolLib.sol:107-119, SSVStaking.sol:183-207 |
| XG-037 | Migrate -> fee change -> removeOperator -> migrate second cluster -> syncFees -> claim (accounting chain) | Multi-mutation: fee change updates `ethNetworkFeeIndex`; operator removal changes burn rate; second migration adds to `daoTotalEthVUnits`; verify `networkTotalEarnings` correctly captures all state transitions and staker claim is accurate | `entry:migrateClusterToETH+updateNetworkFee+removeOperator+migrateClusterToETH+syncFees+claimEthRewards; version:ssv->eth; eb:implicit; cluster:active; ops:4; stakers:1; remove_mode:real; revert:no` | [ ] | ProtocolLib.sol:40-46+65-68+84-89+107-119, OperatorLib.sol:233-262, SSVStaking.sol:183-207 |
| XG-038 | Stake -> migrate -> advance 10000 blocks -> syncFees -> claim -> verify ETH_DEDUCTED_DIGITS truncation | Large fee accrual from migrated cluster; payout truncated to 100K-wei granularity; verify remainder preserved in `accrued[user]` and not lost | `entry:stake+migrateClusterToETH+syncFees+claimEthRewards; version:ssv->eth; eb:implicit; cluster:active; ops:4; stakers:1; remove_mode:none; revert:no` | [ ] | SSVStaking.sol:122 (payout truncation), SSVStaking.sol:144-145 (remainder handling) |
| XG-039 | Migrate -> claimEthRewards exceeds stakingEthPoolBalance -> revert InsufficientBalance | Artificially: staker's accrued rewards exceed what the pool can pay (e.g., rounding inflation from stranded deviation); verify `InsufficientBalance` revert | `entry:migrateClusterToETH+syncFees+claimEthRewards; version:ssv->eth; eb:explicit; cluster:active; ops:4; stakers:1; remove_mode:real; revert:yes` | [ ] | SSVStaking.sol:137-139 (pool balance check), ProtocolLib.sol:84-89 |
| XG-040 | Migrate -> syncFees with `totalStaked == 0` -> fees from migrated cluster lost (BUG-6 + migration) | No cSSV minted; migrated cluster generates ETH network fees; `_syncFees` updates `stakingEthPoolBalance` but `accEthPerShare` stays 0; fees permanently orphaned | `entry:migrateClusterToETH+syncFees; version:ssv->eth; eb:implicit; cluster:active; ops:4; stakers:0; remove_mode:none; revert:no` | [ ] | SSVStaking.sol:199-203 (totalStaked == 0 guard), ProtocolLib.sol:84-89 |
| XG-041 | Migrate cluster A (implicit EB) + migrate cluster B (explicit EB) -> syncFees -> claim | Mixed EB: cluster A contributes baseline vUnits, cluster B contributes baseline + deviation; verify `daoTotalEthVUnits` = A_baseline + B_baseline + B_deviation and staker rewards reflect combined rate | `entry:migrateClusterToETH+migrateClusterToETH+syncFees+claimEthRewards; version:ssv->eth; eb:mixed; cluster:active; ops:4; stakers:1; remove_mode:none; revert:no` | [ ] | SSVClusters.sol:287+304-327, ProtocolLib.sol:84-89+107-119, SSVStaking.sol:183-207 |
| XG-042 | Migrate -> EB update -> operator removed -> liquidate -> verify accounting excludes removed op | Full chain: migrate, oracle updates EB, operator removed, cluster liquidated; verify `_executeLiquidation` deviation accounting handles removed operator's stranded `operatorEthVUnits` correctly | `entry:migrateClusterToETH+updateClusterBalance+removeOperator+liquidate; version:ssv->eth; eb:explicit; cluster:active->liquidated; ops:4; stakers:1; remove_mode:real; revert:no` | [ ] | SSVClusters.sol:564-596 (deviation loop unconditionally iterates removed op), OperatorLib.sol:247 (removed op guard) |
| XG-043 | Migrate with 13 operators -> syncFees -> claim (max operator count fee aggregation) | Maximum operator set; burn rate = sum of 13 ethFees; `daoTotalEthVUnits` includes all 13 ops' validator contributions; verify no truncation or overflow in `networkTotalEarnings` | `entry:migrateClusterToETH+syncFees+claimEthRewards; version:ssv->eth; eb:implicit; cluster:active; ops:13; stakers:1; remove_mode:none; revert:no` | [ ] | OperatorLib.sol:343-384, ProtocolLib.sol:84-89, SSVStaking.sol:183-207 |
| XG-044 | Migrate -> stake -> requestUnstake (full) -> liquidate -> withdrawUnlocked -> verify no interaction | Unstaked staker withdraws SSV after migrated cluster is liquidated; verify withdrawal uses SSV pool (not ETH pool), no cross-contamination between staking pools | `entry:stake+migrateClusterToETH+requestUnstake+liquidate+withdrawUnlocked; version:ssv->eth; eb:implicit; cluster:active->liquidated; ops:4; stakers:1; remove_mode:none; revert:no` | [ ] | SSVStaking.sol:98-108 (SSV token transfer), SSVClusters.sol:552-612 (ETH transfer) |
| XG-045 | Double migration prevention + staking: migrate -> attempt re-migrate -> verify staker rewards unaffected | Second migration reverts `ClusterDoesNotExist`; staker's accrued rewards and `accEthPerShare` unchanged by the failed tx | `entry:migrateClusterToETH+migrateClusterToETH+syncFees+claimEthRewards; version:ssv->eth; eb:implicit; cluster:migrated; ops:4; stakers:1; remove_mode:none; revert:yes` | [ ] | SSVClusters.sol:263+301-302, SSVStaking.sol:113-151 |
| XG-046 | Migrate -> `ethDaoBalance` updated via `updateDAOEarnings` -> syncFees reads `networkTotalEarnings` -> verify consistency | Migration calls `updateDAO` which calls `updateDAOEarnings`; this snapshots `ethDaoBalance` at migration block; subsequent `_syncFees` reads `networkTotalEarnings`; verify no double-counting of the migration-block earnings | `entry:migrateClusterToETH+syncFees; version:ssv->eth; eb:implicit; cluster:active; ops:4; stakers:1; remove_mode:none; revert:no` | [ ] | ProtocolLib.sol:65-68+84-89+107-119, SSVStaking.sol:186-188 |
| XG-047 | Migrate + large validator count + explicit high EB -> syncFees -> claim (overflow boundary) | Stress: 500 validators * 2048 ETH EB; `daoTotalEthVUnits` = 500 * ebToVUnits(1024000); verify `_safeUint64` in `networkTotalEarnings` does not revert or silently truncate over reasonable block ranges | `entry:migrateClusterToETH+updateClusterBalance+syncFees+claimEthRewards; version:ssv->eth; eb:explicit; cluster:active; ops:4; stakers:1; remove_mode:none; revert:no` | [ ] | ProtocolLib.sol:88-89 (_safeUint64), SSVClusters.sol:453-458 (MAX_EB check) |
| XG-048 | Migrate cluster -> claim dust -> verify remainder handling with cSSV balance > 0 | Post-migration, small fee accrual yields `accrued < ETH_DEDUCTED_DIGITS`; claim with `payout == 0` but user holds cSSV → revert `NothingToClaim` (remainder preserved for future claim) | `entry:migrateClusterToETH+syncFees+claimEthRewards; version:ssv->eth; eb:implicit; cluster:active; ops:4; stakers:1; remove_mode:none; revert:yes` | [ ] | SSVStaking.sol:124-131 (dust path with cSSV balance > 0) |
| XG-049 | SSV cluster liquidated -> migrate (reactivation) -> concurrent staker activity -> claim | Staker stakes cSSV, SSV cluster is liquidated (no ETH fees); cluster migrates to ETH (reactivation); staker's rewards should only include post-migration ETH fees, zero from the SSV era | `entry:stake+liquidateSSV+migrateClusterToETH+syncFees+claimEthRewards; version:ssv->eth; eb:implicit; cluster:liquidated; ops:4; stakers:1; remove_mode:none; revert:no` | [ ] | SSVClusters.sol:70-124 (liquidateSSV), SSVClusters.sol:259-343, SSVStaking.sol:183-207 |
| XG-050 | Migrate -> syncFees -> claimEthRewards -> verify packed ETH arithmetic consistency (pack/unpack round-trip) | After migration generates fees, `_syncFees` unpacks `newFees` for `accEthPerShare` calculation; `claimEthRewards` packs `payout` for pool/DAO subtraction; verify pack(unpack(x)) == x and no precision leakage between staking and protocol accounting | `entry:migrateClusterToETH+syncFees+claimEthRewards; version:ssv->eth; eb:implicit; cluster:active; ops:4; stakers:1; remove_mode:none; revert:no` | [ ] | SSVStaking.sol:133+146-147 (PackedETHLib.pack used), ProtocolLib.sol:84-89 (PackedETH arithmetic) |

---

## Detailed Scenario Blocks (10 Most Complex)

### XG-001: Happy Path — Stake, Migrate, SyncFees, Claim

**Category:** End-to-end migration-to-staking flow
**Cross-ref:** MG-001, ST-016

**Setup:**
1. User A stakes 10_000_000_000 SSV -> receives 10e9 cSSV
2. Register 4 operators with SSV fee > 0
3. Create SSV cluster with [op1-op4], 3 validators, sufficient SSV balance
4. ETH network fee set to non-zero value (e.g., `1_000_000` packed)
5. No existing ETH clusters (staking pool starts at 0)

**Steps:**
1. User A calls `stake(10_000_000_000)` -> `cSSV.balanceOf(A) == 10e9`
2. `_syncFees` runs inside `stake`: `current == previous == 0` (no ETH clusters yet) -> `accEthPerShare` stays 0
3. Cluster owner calls `migrateClusterToETH([op1-op4], cluster) { value: 10 ether }`
4. Migration effect on DAO (ProtocolLib.sol:107-119):
   - `updateDAOSSV(false, 3)`: SSV validator count decremented
   - `updateDAO(true, 3)`: `ethDaoValidatorCount += 3`, `daoTotalEthVUnits += 30000`
5. Advance 100 blocks via `vm.roll(block.number + 100)`
6. Call `syncFees()`:
   - `networkTotalEarnings(sp)`: `earningsUnits = (100 * ethNetworkFee * 30000) / 10000`
   - `current = ethDaoBalance + earningsUnits * ETH_DEDUCTED_DIGITS`
   - `newFeesWei = unpack(current - previous)`
   - `accEthPerShare += (newFeesWei * 1e18) / 10e9`
7. User A calls `claimEthRewards()`:
   - `_syncFees`: no new blocks since step 6 if same block; otherwise re-syncs
   - `_settle(A)`: `pending = 10e9 * accEthPerShare / 1e18`
   - `payout = pending - (pending % 100_000)`
   - Both `stakingEthPoolBalance` and `ethDaoBalance` decremented by `packed(payout)`
8. Verify User A received `payout` ETH

**Critical assertions:**
1. Before migration: `syncFees` produces zero new fees (no ETH clusters)
2. After migration: `daoTotalEthVUnits == 30000` (3 validators * BPS_DENOMINATOR)
3. `accEthPerShare` reflects exactly the fees generated by the migrated cluster's network fee contribution
4. `payout` is correctly truncated to 100K-wei granularity
5. `stakingEthPoolBalance == networkTotalEarnings - payout` after claim
6. SSV-era fees (from the SSV cluster before migration) do NOT appear in ETH staking pool

**File References:** `SSVClusters.sol:259-343`, `ProtocolLib.sol:84-89+107-119`, `SSVStaking.sol:43-60+183-207+113-151`

---

### XG-007: Migrate -> Liquidate -> SyncFees -> Claim (Frozen Rewards)

**Category:** Fee accrual cessation on liquidation
**Cross-ref:** MG-055, ST-070, ST-071

**Setup:**
1. User A stakes 1e12 SSV -> 1e12 cSSV
2. Register 4 operators, create SSV cluster with 2 validators
3. Migrate cluster to ETH with minimal balance (just above liquidation threshold)
4. ETH network fee set to meaningful value
5. `minimumBlocksBeforeLiquidation = 100`

**Steps:**
1. Stake and sync (no ETH fees yet)
2. Migrate cluster with `msg.value = 0.01 ether` (barely sufficient)
3. Advance 50 blocks -> `syncFees()` -> record `accEthPerShare_prelude`
4. Advance more blocks until cluster becomes liquidatable
5. Liquidator calls `liquidate(owner, operatorIds, cluster)`:
   - `updateClusterOperators(false, 2)`: `ethValidatorCount -= 2` for each op
   - `_executeLiquidation`: `updateDAO(false, 2)` -> `daoTotalEthVUnits -= 20000`
   - Remaining cluster balance transferred to liquidator
6. Advance 100 more blocks -> `syncFees()`:
   - `networkTotalEarnings`: with `daoTotalEthVUnits == 0`, `earningsUnits == 0`
   - `current == ethDaoBalance` (frozen at liquidation-time snapshot)
   - `current <= previous` -> `accEthPerShare` unchanged
7. User A calls `claimEthRewards()`:
   - `pending = 1e12 * accEthPerShare_prelude / 1e18`
   - Gets only pre-liquidation rewards

**Critical assertions:**
1. `daoTotalEthVUnits` drops to 0 after liquidation (assuming only cluster)
2. `networkTotalEarnings` stops growing after liquidation
3. `accEthPerShare` frozen at liquidation-time value
4. Staker's claim reflects exactly the fees generated between migration and liquidation
5. Liquidator received cluster's remaining ETH (separate from staking pool)
6. `stakingEthPoolBalance` NOT reduced by liquidation (liquidation transfers from cluster balance, not pool)

**File References:** `SSVClusters.sol:31-65+552-612`, `ProtocolLib.sol:107-119+84-89`, `SSVStaking.sol:183-207+113-151`

---

### XG-011: Full Unstake During Migration-Era — BUG-6 Interaction

**Category:** Zero cSSV supply with active migrated cluster
**Cross-ref:** ST-076 (BUG-6), MG-001

**Setup:**
1. User A stakes `MINIMAL_STAKING_AMOUNT` SSV
2. Migrate SSV cluster to ETH (generating network fees)
3. ETH network fee non-zero

**Steps:**
1. User A calls `stake(1_000_000_000)` -> 1e9 cSSV
2. Migrate SSV cluster to ETH -> `daoTotalEthVUnits` increases
3. Advance 50 blocks -> `syncFees()` -> `accEthPerShare` increases (fees from migration)
4. User A calls `requestUnstake(1_000_000_000)`:
   - `_settleWithBalance(A, 1e9, s)`: settles pending to `accrued[A]`
   - `cSSV.burn(A, 1e9)` -> `totalSupply == 0`
5. Advance 200 blocks (migrated cluster continues generating ETH network fees)
6. Call `syncFees()`:
   - `networkTotalEarnings` returns higher value (200 blocks of accrual)
   - `newFeesWei > 0`
   - But `totalStaked == 0` -> `accEthPerShare` NOT updated
   - `stakingEthPoolBalance = current` (updated, inflated)
7. User B calls `stake(1_000_000_000)` -> 1e9 cSSV
8. Advance 50 blocks -> `syncFees()`:
   - `accEthPerShare` increases only by fees from these 50 blocks
   - The 200-block gap fees are permanently orphaned in `stakingEthPoolBalance`
9. User B calls `claimEthRewards()`:
   - Gets rewards from 50-block period only, NOT the 200-block gap

**Critical assertions:**
1. Fees from step 5 (200 blocks) are lost to stakers (BUG-6 confirmed for migration-generated fees)
2. `stakingEthPoolBalance` is inflated relative to sum of all possible claims
3. User A's accrued from step 4 is still claimable (settled before burn)
4. User B's rewards are strictly proportional to post-re-stake accrual
5. Invariant: `Sigma(all_claims) < stakingEthPoolBalance` (strict less-than due to lost fees)

**File References:** `SSVStaking.sol:199-203`, `ProtocolLib.sol:84-89`, `SSVClusters.sol:259-343`

---

### XG-013: Stranded Deviation from Removed Operator (Potential Over-Reward)

**Category:** Accounting inconsistency — deviation on removed operator inflates DAO earnings
**Cross-ref:** MG-052 (stranded deviation), ST-068

**Setup:**
1. Register 4 operators (op1-op4), all with SSV fee > 0
2. Create SSV cluster with 2 validators
3. Set explicit EB on SSV cluster: `updateClusterBalance` with effectiveBalance = 96 ETH
   - `vUnits = ebToVUnits(96)` = 30000
   - `baseline = 2 * 10000 = 20000`, `deviation = 10000`
4. Remove op4 via `removeOperator(op4)` (zeroes both snapshot blocks)
5. User A stakes 1e12 SSV

**Steps:**
1. Migrate cluster:
   - `updateClusterOperatorsMigration`: op4 skipped (line 363-364)
   - `updateDAO(true, 2)`: `daoTotalEthVUnits += 20000` (baseline)
   - Deviation loop (SSVClusters.sol:319-322): iterates ALL 4 ops including op4
   - `seb.operatorEthVUnits[op4] += 10000` (stranded)
   - `sp.daoTotalEthVUnits += 10000` (deviation)
   - Total `daoTotalEthVUnits = 30000`
2. But only 3 operators actually serve the cluster (op4 removed)
3. Advance 100 blocks -> `syncFees()`:
   - `networkTotalEarnings`: uses `daoTotalEthVUnits = 30000`
   - Earnings assume 30000 vUnits of service, but actual cluster fees accrue at rate for 3 ops
4. User A calls `claimEthRewards()`:
   - Gets rewards based on `daoTotalEthVUnits = 30000`
   - But cluster's actual burn rate only includes 3 ops' fees
   - **Potential over-reward**: staking pool thinks more network fees are being generated than clusters are actually paying

**Critical assertions:**
1. `daoTotalEthVUnits` includes stranded deviation (10000) from removed op4
2. `networkTotalEarnings` is inflated by the stranded deviation
3. Cluster's actual fee deduction (burn rate) only includes 3 operators
4. Over time, `stakingEthPoolBalance` may exceed actual ETH available for claims
5. This is a known design trade-off — verify whether `claimEthRewards` can ever hit `InsufficientBalance` due to this inflation

**File References:** `SSVClusters.sol:319-322`, `ProtocolLib.sol:84-89`, `SSVStaking.sol:137-139+183-207`

---

### XG-015: Multiple Migrated Clusters — One Liquidated, Reward Rate Adjusts

**Category:** Partial liquidation effect on staking rewards
**Cross-ref:** MG-044, ST-071

**Setup:**
1. User A stakes 1e12 SSV -> 1e12 cSSV
2. Register 4 operators (shared)
3. Create SSV cluster A (Alice, 3 validators, 1 ETH balance) and cluster B (Bob, 5 validators, 10 ETH balance)
4. Migrate both clusters:
   - Cluster A: `daoTotalEthVUnits += 30000`
   - Cluster B: `daoTotalEthVUnits += 50000`
   - Total: `daoTotalEthVUnits = 80000`
5. ETH network fee = 1_000_000 (packed)

**Steps:**
1. Advance 100 blocks -> `syncFees()`:
   - `earningsUnits = (100 * 1_000_000 * 80000) / 10000 = 800_000_000`
   - Record `accEthPerShare_1`
2. Cluster A balance depletes, liquidated:
   - `updateDAO(false, 3)`: `daoTotalEthVUnits -= 30000`
   - `daoTotalEthVUnits = 50000` (only cluster B remains)
3. Advance another 100 blocks -> `syncFees()`:
   - `earningsUnits = (100 * 1_000_000 * 50000) / 10000 = 500_000_000`
   - `accEthPerShare_2 - accEthPerShare_1` is smaller than step 1's increment
4. User A claims: total reward = sum of both periods at their respective rates

**Critical assertions:**
1. After cluster A liquidation: `daoTotalEthVUnits` drops from 80000 to 50000
2. `networkTotalEarnings` growth rate decreases proportionally (50000/80000 = 62.5% of prior rate)
3. Staker's rewards for period 2 are exactly 62.5% of period 1's rate (per block)
4. Shared operators' `ethValidatorCount` correctly reflects only cluster B after liquidation
5. Total claimed does not exceed fees actually generated across both periods

**File References:** `SSVClusters.sol:552-612`, `ProtocolLib.sol:107-119+84-89`, `SSVStaking.sol:183-207+113-151`

---

### XG-019: SyncFees Sandwich Around Migration (Index Recalculation)

**Category:** `ethDaoIndexBlockNumber` state transition during migration
**Cross-ref:** MG-033, ST-011

**Setup:**
1. Pre-existing ETH cluster with 2 validators (generating fees)
2. `daoTotalEthVUnits = 20000`
3. User A has staked cSSV
4. SSV cluster with 3 validators ready to migrate

**Steps:**
1. Advance 50 blocks
2. Call `syncFees()` (pre-migration sync):
   - `networkTotalEarnings`: `earningsUnits = (50 * ethNetworkFee * 20000) / BPS_DENOMINATOR`
   - `ethDaoBalance` updated, `ethDaoIndexBlockNumber = block.number`
   - `accEthPerShare` increases by `newFeesWei * 1e18 / totalStaked`
   - Record `accEthPerShare_pre`
3. In same or next block, migrate SSV cluster:
   - `updateDAO(true, 3)` internally calls `updateDAOEarnings`:
     - `ethDaoBalance = networkTotalEarnings(sp)` at migration block
     - `ethDaoIndexBlockNumber = block.number` (reset)
   - `daoTotalEthVUnits += 30000` -> total now 50000
4. Advance 50 more blocks
5. Call `syncFees()` (post-migration sync):
   - `networkTotalEarnings`: `earningsUnits = (50 * ethNetworkFee * 50000) / BPS_DENOMINATOR`
   - This uses the NEW `daoTotalEthVUnits` and the reset `ethDaoIndexBlockNumber`
   - `accEthPerShare` increases by post-migration delta

**Critical assertions:**
1. Pre-migration `syncFees` captures fees at the old rate (20000 vUnits)
2. Migration's `updateDAOEarnings` snapshots the DAO balance at migration block
3. Post-migration `syncFees` captures fees at the new rate (50000 vUnits)
4. No double-counting: the migration block's earnings are counted exactly once
5. `ethDaoIndexBlockNumber` reset by migration prevents retroactive application of new vUnits to pre-migration blocks
6. Total `accEthPerShare` = sum of both deltas (old rate * 50 blocks + new rate * 50 blocks)

**File References:** `ProtocolLib.sol:65-68+84-89+107-119`, `SSVStaking.sol:183-207`, `SSVClusters.sol:287`

---

### XG-028: Two-Phase EB Change — Reward Rate Up Then Down

**Category:** EB volatility effect on staking rewards
**Cross-ref:** ST-068, MG-005

**Setup:**
1. User A stakes 1e12 SSV
2. Register 4 operators, migrate SSV cluster with 2 validators (implicit EB = 64 ETH total, vUnits = 20000)
3. `daoTotalEthVUnits = 20000`

**Steps:**
1. Advance 50 blocks -> `syncFees()` -> record `reward_rate_1 = newFeesWei / 50`
2. Oracle updates EB to 128 ETH total (64 ETH/validator):
   - `newVUnits = ebToVUnits(128) = 40000`
   - `updateDAOEthVUnits(20000, 40000)`: `daoTotalEthVUnits += 20000` -> total 40000
3. Advance 50 blocks -> `syncFees()` -> record `reward_rate_2 = newFeesWei / 50`
4. Verify `reward_rate_2 == 2 * reward_rate_1` (double vUnits = double rate)
5. User A calls `claimEthRewards()` -> gets combined rewards from both periods
6. Oracle updates EB to 64 ETH total (32 ETH/validator, back to default):
   - `newVUnits = ebToVUnits(64) = 20000`
   - `updateDAOEthVUnits(40000, 20000)`: `daoTotalEthVUnits -= 20000` -> total 20000
7. Advance 50 blocks -> `syncFees()` -> record `reward_rate_3`
8. Verify `reward_rate_3 == reward_rate_1` (back to original)
9. User A calls `claimEthRewards()` -> gets period 3 rewards only (period 1+2 already claimed)

**Critical assertions:**
1. `daoTotalEthVUnits` correctly tracks both increases and decreases
2. `networkTotalEarnings` rate changes immediately after EB update (within same `updateDAOEthVUnits` call which snapshots via `updateDAOEarnings`)
3. Staker rewards for each period match the period's `daoTotalEthVUnits` rate
4. No precision loss in the up-then-down cycle (20000 -> 40000 -> 20000)
5. After claim in step 5, `accrued[A]` reflects only the truncation remainder

**File References:** `SSVClusters.sol:386-417`, `ProtocolLib.sol:142-150+84-89`, `SSVStaking.sol:183-207+113-151`

---

### XG-035: Explicit EB Liquidation — Deviation Cleanup and Staker Impact

**Category:** Deviation accounting during liquidation
**Cross-ref:** MG-052, ST-070

**Setup:**
1. User A stakes 1e12 SSV
2. Register 4 operators, migrate SSV cluster with 2 validators
3. Oracle updates EB to 128 ETH total:
   - `vUnits = 40000`, `baseline = 20000`, `deviation = 20000`
   - `daoTotalEthVUnits = 40000`
   - Each `operatorEthVUnits[opN] = 20000`
4. Cluster has low ETH balance (near liquidation threshold)

**Steps:**
1. Advance blocks until cluster is liquidatable
2. `syncFees()` -> record `accEthPerShare_pre` (using daoTotalEthVUnits = 40000)
3. Liquidator calls `liquidate(owner, operatorIds, cluster)`:
   - `updateClusterOperators(false, 2)`: `ethValidatorCount -= 2` for each op
   - `_executeLiquidation`:
     - `updateDAO(false, 2)`: `daoTotalEthVUnits -= 20000` (baseline removed)
     - `ebSnapshot.vUnits = 40000`, `baseline = 20000`, deviation = 20000
     - `sp.daoTotalEthVUnits -= 20000` (deviation removed)
     - For each op: `seb.operatorEthVUnits[opN] -= 20000`
   - Final: `daoTotalEthVUnits = 0`
4. Advance blocks -> `syncFees()`:
   - `networkTotalEarnings`: `earningsUnits = 0` (daoTotalEthVUnits = 0)
   - `current == ethDaoBalance` (frozen)
   - `accEthPerShare` unchanged
5. User A claims -> gets only pre-liquidation rewards

**Critical assertions:**
1. Both baseline (via `updateDAO`) and deviation (via `_executeLiquidation` loop) removed from `daoTotalEthVUnits`
2. Each operator's `operatorEthVUnits` correctly zeroed
3. `networkTotalEarnings` growth completely stops (daoTotalEthVUnits = 0)
4. Staker's total claim matches fees generated during the active period
5. `stakingEthPoolBalance` is NOT reduced by liquidation (liquidation uses cluster balance)

**File References:** `SSVClusters.sol:552-612`, `ProtocolLib.sol:107-119+84-89`, `SSVStaking.sol:183-207+113-151`

---

### XG-040: BUG-6 with Migration — Fees Lost During Zero Supply

**Category:** Known bug interaction with migration-generated fees
**Cross-ref:** ST-076, XG-011

**Setup:**
1. No cSSV minted (totalSupply = 0)
2. Register 4 operators, create SSV cluster with 3 validators
3. ETH network fee non-zero

**Steps:**
1. Migrate SSV cluster to ETH -> `daoTotalEthVUnits = 30000`
2. Advance 1000 blocks (significant fee accrual with no stakers)
3. Call `syncFees()`:
   - `networkTotalEarnings`: `earningsUnits = (1000 * ethNetworkFee * 30000) / 10000`
   - `current > previous` (fees accrued)
   - `totalStaked = cSSV.totalSupply() = 0`
   - `accEthPerShare` NOT updated (guarded by `totalStaked != 0`)
   - `stakingEthPoolBalance = current` (inflated by 1000 blocks of fees)
4. User A stakes `1_000_000_000` SSV -> 1e9 cSSV
5. Advance 100 blocks -> `syncFees()`:
   - `current = networkTotalEarnings` (now includes 1100 total blocks of accrual)
   - `previous = stakingEthPoolBalance` (set in step 3, includes 1000 blocks)
   - `newFeesWei = current - previous` (only 100 blocks of new fees)
   - `accEthPerShare += (100_block_fees * 1e18) / 1e9`
6. User A calls `claimEthRewards()` -> gets rewards from 100 blocks only
7. The 1000 blocks of fees are permanently orphaned

**Critical assertions:**
1. 1000 blocks of migration-generated fees are lost (cannot be claimed by anyone)
2. `stakingEthPoolBalance` is inflated by orphaned fees
3. `accEthPerShare` only reflects post-staking fee accrual
4. A malicious actor cannot exploit the orphaned fees
5. `claimEthRewards` will never revert `InsufficientBalance` due to orphaned fees (they inflate the pool, making it always sufficient)

**File References:** `SSVStaking.sol:199-203`, `ProtocolLib.sol:84-89`, `SSVClusters.sol:259-343`

---

### XG-042: Full Chain — Migrate, EB Update, Remove Operator, Liquidate

**Category:** Multi-mutation accounting chain
**Cross-ref:** MG-018, MG-052, XG-013

**Setup:**
1. Register 4 operators (op1-op4), all with SSV fee > 0
2. Create SSV cluster with 2 validators
3. User A stakes 1e12 SSV

**Steps:**
1. Migrate cluster to ETH with 1 ETH:
   - `daoTotalEthVUnits = 20000` (baseline, implicit EB)
   - All 4 ops: `ethValidatorCount = 2`
2. Oracle updates EB to 96 ETH (48/validator):
   - `newVUnits = 30000`, `storedVUnits = 20000`
   - `updateDAOEthVUnits(20000, 30000)`: `daoTotalEthVUnits += 10000` -> total 30000
   - Each op: `operatorEthVUnits += 10000`
3. `syncFees()` -> record `rate_1` (using daoTotalEthVUnits = 30000)
4. Remove op4:
   - `op4.ethSnapshot.block = 0`, `op4.ethFee = 0`, `op4.ethValidatorCount = 0`
   - `op4`'s `operatorEthVUnits` preserved (stranded deviation of 10000)
   - `daoTotalEthVUnits` unchanged (removal doesn't touch DAO vUnits directly)
5. Advance blocks until cluster is liquidatable (with 3-op burn rate)
6. Liquidate cluster:
   - `updateClusterOperators(false, 2)`: only op1-op3 updated (op4 skipped, block=0)
   - `_executeLiquidation`:
     - `updateDAO(false, 2)`: `daoTotalEthVUnits -= 20000` (baseline)
     - `vUnitsCluster = 30000`, `baseline = 20000`, `deviation = 10000`
     - `sp.daoTotalEthVUnits -= 10000` (deviation)
     - Loop: `operatorEthVUnits[op4] -= 10000` (removed op's stranded deviation cleaned up)
   - Final: `daoTotalEthVUnits = 0`
7. `syncFees()` -> `accEthPerShare` frozen
8. User A claims -> gets rewards from migration through liquidation

**Critical assertions:**
1. EB update correctly increased `daoTotalEthVUnits` and per-operator vUnits
2. Operator removal does NOT reduce `daoTotalEthVUnits` (stranded deviation remains)
3. Liquidation's `_executeLiquidation` cleans up BOTH baseline and deviation
4. Op4's stranded `operatorEthVUnits` is subtracted during liquidation (line 587)
5. Final `daoTotalEthVUnits == 0` (all accounting cleaned up)
6. Staker's total claim covers fees from migration to liquidation at the correct rate

**File References:** `SSVClusters.sol:348-417+552-612`, `ProtocolLib.sol:142-150+107-119`, `OperatorLib.sol:233-262`, `SSVStaking.sol:183-207+113-151`

---

## Coverage Matrix

| Dimension | XG Scenarios |
|-----------|-------------|
| **Migration -> staking reward flow** | XG-001, XG-002, XG-003, XG-024, XG-025 |
| **Migration + EB change -> reward rate** | XG-004, XG-005, XG-028, XG-029, XG-041 |
| **Migration + network fee change** | XG-006, XG-026, XG-031, XG-037 |
| **Migration + liquidation -> reward freeze** | XG-007, XG-008, XG-009, XG-015, XG-018, XG-034, XG-035, XG-036 |
| **Migration + removed operator** | XG-012, XG-013, XG-020, XG-042 |
| **Migration + unstake/cSSV dynamics** | XG-010, XG-011, XG-023, XG-044 |
| **BUG-6 (zero supply) interaction** | XG-011, XG-040 |
| **Precision and overflow** | XG-016, XG-029, XG-038, XG-047, XG-048, XG-050 |
| **Multi-cluster migration** | XG-014, XG-015, XG-041, XG-045 |
| **Same-block / atomicity** | XG-019, XG-033, XG-046 |
| **Pool balance consistency** | XG-030, XG-032, XG-039 |
| **0-validator / edge** | XG-027, XG-049 |

---

## Summary

- **Total scenarios:** 50 (XG-001 through XG-050)
- **Detailed blocks:** 10 (XG-001, XG-007, XG-011, XG-013, XG-015, XG-019, XG-028, XG-035, XG-040, XG-042)
- **Revert scenarios:** 3 (XG-039, XG-045, XG-048)
- **Entry point combinations covered:**
  - `migrateClusterToETH + syncFees + claimEthRewards` (core flow): 22 scenarios
  - `migrateClusterToETH + liquidate + syncFees`: 10 scenarios
  - `migrateClusterToETH + updateClusterBalance + syncFees`: 8 scenarios
  - `migrateClusterToETH + removeOperator + syncFees`: 4 scenarios
  - `migrateClusterToETH + requestUnstake/onCSSVTransfer`: 4 scenarios
  - `migrateClusterToETH + updateNetworkFee + syncFees`: 3 scenarios
- **Known edge cases covered:** BUG-6 with migration fees, stranded deviation on removed operators, EB volatility effect on rewards, pack/unpack round-trip precision, `ethDaoIndexBlockNumber` reset during migration, zero-validator migration, same-block race between migration and sync, MAX_EB overflow boundary
- **W1 cross-references:** MG-001, MG-005, MG-008, MG-018, MG-033, MG-044, MG-052, MG-055, ST-011, ST-016, ST-060, ST-068, ST-070, ST-071, ST-076
